// questions-extra2.js — Second extended bank (added to the base bank)
// Domain 1 (ids 131-145), Domain 2 (226-238), Domain 3 (326-338), Domain 4 (421-431)
QUESTIONS.push(
  {"id": 131, "domain": 1, "q": "A security audit reveals that an application running on EC2 was exploited through a Server-Side Request Forgery (SSRF) attack that queried the instance metadata service to steal IAM role credentials. Which measure BEST mitigates this class of attack?", "options": ["Disable IAM roles on all EC2 instances", "Enforce IMDSv2 (session-token-based metadata service) by setting HttpTokens to required on the instances", "Move the instances to a public subnet", "Enable EBS encryption on the root volumes"], "correct": 1, "explanation": "IMDSv2 requires a session token obtained through a PUT request before any metadata call, which defeats most SSRF attacks because they typically can only issue simple GET requests. Disabling IAM roles would force long-lived credentials, which is worse. Subnet placement and EBS encryption are unrelated to metadata service protection."},
  {"id": 132, "domain": 1, "q": "A web application must let customers download private files stored in S3 for a limited time (15 minutes) after purchase, without making the bucket public and without creating IAM users for the customers. Which solution should you implement?", "options": ["Generate S3 presigned URLs with a 15-minute expiration", "Add a bucket policy allowing s3:GetObject from 0.0.0.0/0", "Create one IAM user per customer with read access", "Enable S3 Transfer Acceleration"], "correct": 0, "explanation": "A presigned URL is signed with the application's credentials and grants temporary access to a specific object until its expiration, which exactly matches the time-limited private download requirement. A public bucket policy exposes all objects to everyone, one IAM user per customer is unmanageable and against best practices, and Transfer Acceleration only speeds up transfers."},
  {"id": 133, "domain": 1, "q": "A compliance team requires that all requests to a sensitive S3 bucket be rejected unless they are made over HTTPS (TLS). Which mechanism enforces this requirement?", "options": ["Enable default SSE-KMS encryption on the bucket", "Add a bucket policy that denies all actions when the condition aws:SecureTransport is false", "Enable S3 Versioning", "Attach a Security Group to the bucket allowing only port 443"], "correct": 1, "explanation": "A bucket policy with a Deny statement on the condition \"aws:SecureTransport\": \"false\" rejects any request made over plain HTTP, enforcing encryption in transit. SSE-KMS protects data at rest, not in transit. Versioning retains object versions. Security Groups cannot be attached to S3 buckets."},
  {"id": 134, "domain": 1, "q": "An operations team needs interactive shell access to EC2 instances in private subnets. The security team prohibits bastion hosts, SSH key management, and any inbound port 22 openings, and requires that every session be logged. Which solution meets all of these requirements?", "options": ["An SSH bastion host in a public subnet with strict Security Groups", "AWS Systems Manager Session Manager with session logging to S3 or CloudWatch Logs", "EC2 Instance Connect with port 22 open to the corporate CIDR", "A site-to-site VPN with direct SSH to the instances"], "correct": 1, "explanation": "Session Manager provides shell access through the SSM agent with no inbound ports, no bastion, and no SSH keys, with IAM-based access control and full session logging to S3 or CloudWatch Logs. A bastion host and EC2 Instance Connect still require port 22 to be reachable, and a VPN with direct SSH keeps key management and lacks built-in session logging."},
  {"id": 135, "domain": 1, "q": "A security team wants to continuously identify S3 buckets, IAM roles, KMS keys, and other resources whose policies grant access to external AWS accounts or to the public, across the organization. Which service is designed for this analysis?", "options": ["AWS IAM Access Analyzer", "Amazon Inspector", "AWS Shield Advanced", "Amazon CloudWatch"], "correct": 0, "explanation": "IAM Access Analyzer uses automated reasoning to analyze resource policies and generate findings whenever a resource is accessible from outside the zone of trust (account or organization). Inspector scans for software vulnerabilities, Shield Advanced protects against DDoS, and CloudWatch handles metrics and logs."},
  {"id": 136, "domain": 1, "q": "A platform team lets developers create their own IAM roles for applications, but wants to guarantee that those roles can never exceed a defined set of maximum permissions, regardless of the policies the developers attach. Which IAM feature enforces this limit?", "options": ["Service Control Policies (SCPs)", "IAM permissions boundaries attached to the roles created by the developers", "Inline policies on the developer users", "AWS Config rules"], "correct": 1, "explanation": "A permissions boundary is an advanced IAM feature that sets the maximum permissions an identity-based policy can grant to a role or user; effective permissions are the intersection of the boundary and the attached policies. SCPs apply at the account/OU level, not per delegated role creation within an account. Inline policies do not constrain other policies, and Config only detects issues after the fact."},
  {"id": 137, "domain": 1, "q": "A company wants to automatically detect Security Groups that allow SSH (port 22) from 0.0.0.0/0 and immediately remove the offending rule without human intervention. Which combination achieves this?", "options": ["An AWS Config managed rule (restricted-ssh) with an SSM Automation remediation action", "Amazon GuardDuty with an SNS notification", "AWS Trusted Advisor checks reviewed weekly", "VPC Flow Logs analyzed manually"], "correct": 0, "explanation": "AWS Config evaluates resources against rules such as restricted-ssh and can trigger an SSM Automation document as an automatic remediation to remove the non-compliant rule. GuardDuty detects threats but does not evaluate SG configuration compliance, Trusted Advisor reviews are manual, and Flow Logs analysis does not remediate anything."},
  {"id": 138, "domain": 1, "q": "After a suspected intrusion, a security team must analyze which IP addresses communicated with specific EC2 instances, on which ports, and whether the traffic was accepted or rejected. Which data source provides this information?", "options": ["AWS CloudTrail", "VPC Flow Logs", "AWS Config configuration history", "S3 server access logs"], "correct": 1, "explanation": "VPC Flow Logs capture metadata about IP traffic to and from network interfaces (source/destination IP, port, protocol, accept/reject), exactly what is needed for network forensics. CloudTrail records API calls, not packet flows. Config tracks resource configuration changes, and S3 access logs only cover requests to S3 buckets."},
  {"id": 139, "domain": 1, "q": "A company encrypts data with AWS KMS in us-east-1 and replicates it to eu-west-1 for disaster recovery. It must be able to decrypt the data in the secondary Region without re-encrypting everything and without cross-Region KMS API calls. Which KMS capability addresses this?", "options": ["KMS multi-Region keys (a replica key in eu-west-1 with the same key material)", "Sharing the KMS key through a resource policy across Regions", "Exporting the key material to an EC2 instance in eu-west-1", "Using SSE-S3 instead of KMS"], "correct": 0, "explanation": "KMS multi-Region keys are a set of interoperable keys with the same key material and key ID in different Regions, allowing data encrypted in one Region to be decrypted with the replica key in another without cross-Region calls. A KMS key itself cannot be used outside its Region through a policy. Exporting key material to EC2 defeats KMS security, and SSE-S3 removes customer key control."},
  {"id": 140, "domain": 1, "q": "A company with dozens of AWS accounts in an Organization wants to centrally deploy and enforce AWS WAF rules and Security Group policies across all accounts, with automatic remediation when a policy is violated in any account. Which service should it use?", "options": ["AWS Firewall Manager", "AWS WAF deployed manually in each account", "Amazon GuardDuty", "AWS Network Firewall in a single account"], "correct": 0, "explanation": "Firewall Manager centrally configures and audits WAF rules, Shield Advanced protections, Security Group policies, and Network Firewall across all accounts of an Organization, with automatic remediation. Manual per-account WAF deployment does not scale and lacks enforcement, GuardDuty detects threats without enforcing rules, and Network Firewall in one account does not govern the others."},
  {"id": 141, "domain": 1, "q": "A network team manages a shared VPC architecture and wants several AWS accounts of the same Organization to launch resources into centrally managed subnets, without duplicating VPCs or setting up peering. Which service enables this sharing?", "options": ["AWS Resource Access Manager (RAM) to share the subnets with the other accounts", "VPC Peering between each account", "AWS PrivateLink", "Copying the VPC configuration with CloudFormation StackSets"], "correct": 0, "explanation": "AWS RAM enables VPC subnet sharing: the owner account manages the VPC while participant accounts launch resources (EC2, RDS...) directly into the shared subnets. Peering connects distinct VPCs but does not share subnets, PrivateLink exposes services rather than subnets, and StackSets would create duplicate, separate VPCs."},
  {"id": 142, "domain": 1, "q": "A development team is building a consumer web application that needs user sign-up, sign-in, password reset, MFA, and the issuance of JWT tokens to secure its REST API. Which service provides these capabilities with the LEAST development effort?", "options": ["Amazon Cognito User Pools", "AWS IAM Identity Center", "AWS STS alone", "A custom authentication service on EC2"], "correct": 0, "explanation": "Cognito User Pools provide a managed user directory with sign-up/sign-in flows, MFA, password recovery, hosted UI, and OIDC/JWT token issuance that integrates with API Gateway and ALBs. IAM Identity Center targets workforce SSO into AWS accounts, STS issues AWS credentials but no user directory, and a custom service requires significant development and security effort."},
  {"id": 143, "domain": 1, "q": "A public API behind CloudFront and an ALB is suffering from an HTTP flood where individual IP addresses send thousands of requests per minute. Legitimate clients send far fewer requests. Which AWS WAF feature mitigates this attack?", "options": ["A rate-based rule that blocks IPs exceeding a request threshold over a time window", "An IP set containing all known good clients", "AWS Shield Standard", "Geo-match rules blocking all foreign countries"], "correct": 0, "explanation": "WAF rate-based rules count requests per source IP over a sliding window and automatically block IPs that exceed the configured threshold, which directly mitigates layer-7 HTTP floods. Maintaining an allowlist of all legitimate clients is impractical for a public API. Shield Standard covers layer 3/4 DDoS, and geo-blocking penalizes legitimate users without stopping distributed floods from allowed countries."},
  {"id": 144, "domain": 1, "q": "A company uses AWS Organizations and must guarantee that API activity is logged in every current AND future member account, delivered to a central, access-controlled S3 bucket in the security account. Which approach requires the LEAST ongoing effort?", "options": ["Create an organization trail in AWS CloudTrail from the management account", "Create an individual trail manually in each member account", "Enable CloudWatch metric streams in all accounts", "Configure AWS Config aggregators"], "correct": 0, "explanation": "An organization trail automatically logs events for all existing and newly added member accounts and delivers them to a central S3 bucket, with member accounts unable to disable it. Manual per-account trails require constant maintenance as accounts are added. CloudWatch metric streams export metrics, not API audit logs, and Config aggregates configuration compliance, not CloudTrail events."},
  {"id": 145, "domain": 1, "q": "An application on EC2 connects to an RDS for PostgreSQL database. The security team wants to eliminate static database passwords stored in configuration files. Which approaches achieve this goal? (Choose TWO answers.)", "multi": true, "options": ["Store the credentials in AWS Secrets Manager with automatic rotation and fetch them at runtime", "Use IAM database authentication with short-lived authentication tokens", "Store the password in a plaintext environment variable on the instance", "Hardcode the password and restrict file permissions to root", "Email the password to the operations team monthly"], "correct": [0, 1], "explanation": "Secrets Manager removes static credentials from configuration by serving and rotating them centrally, and IAM database authentication (supported by RDS MySQL/PostgreSQL) replaces passwords entirely with 15-minute IAM-signed tokens. Plaintext environment variables and hardcoded passwords remain static secrets on the host, and emailing passwords is an obvious anti-pattern."},

  {"id": 226, "domain": 2, "q": "Workers consume an SQS Standard queue and take about 4 minutes to process each message, but the queue uses the default visibility timeout of 30 seconds. The team observes that messages are frequently processed multiple times in parallel. What is the BEST fix?", "options": ["Increase the visibility timeout to a value comfortably above the processing time (e.g., 5-6 minutes)", "Switch to an SNS topic", "Reduce the number of workers to one", "Enable long polling on the queue"], "correct": 0, "explanation": "When the visibility timeout expires before the consumer finishes and deletes the message, SQS makes it visible again and another worker picks it up, causing duplicate processing; setting the timeout above the actual processing time fixes this. SNS does not queue work. A single worker destroys throughput, and long polling reduces empty receives but has no effect on duplicates."},
  {"id": 227, "domain": 2, "q": "A serverless application uses hundreds of concurrent Lambda functions that open connections to an RDS for MySQL database. During spikes, the database hits its max_connections limit and rejects new connections. Which solution addresses this with minimal application changes?", "options": ["Place Amazon RDS Proxy between the Lambda functions and the database to pool and share connections", "Increase the Lambda memory allocation", "Replace RDS with S3", "Lower the Lambda concurrency limit to 5"], "correct": 0, "explanation": "RDS Proxy maintains a pool of established database connections that Lambda invocations share, dramatically reducing the number of direct connections and absorbing spikes; the application only changes its endpoint. More Lambda memory does not reduce connections, S3 is not a relational database, and capping concurrency at 5 throttles the application."},
  {"id": 228, "domain": 2, "q": "A globally distributed application needs a NoSQL database where users in Europe, Asia, and the US can all read AND write locally with single-digit-millisecond latency, with data automatically replicated between Regions. Which solution meets this need?", "options": ["DynamoDB global tables", "A single DynamoDB table with DAX in one Region", "RDS for MySQL with cross-Region read replicas", "ElastiCache for Redis in each Region"], "correct": 0, "explanation": "DynamoDB global tables provide a fully managed multi-Region, multi-active database: each Region accepts local reads and writes that replicate automatically to the others. A single-Region table with DAX only speeds up that Region. Cross-Region RDS read replicas are read-only outside the primary. ElastiCache is a cache, not a replicated source-of-truth database."},
  {"id": 229, "domain": 2, "q": "A company wants its main website to automatically display a static 'maintenance' page hosted on S3/CloudFront whenever the primary application (behind an ALB) fails its health checks. Which Route 53 setup achieves this?", "options": ["A failover routing policy: primary record pointing to the ALB with a health check, secondary record pointing to the CloudFront/S3 static site", "A weighted policy 50/50 between the ALB and the static site", "A latency-based policy between the two endpoints", "A geolocation policy with the static site as default"], "correct": 0, "explanation": "Route 53 failover routing returns the primary record while its associated health check passes and automatically switches to the secondary record (the static maintenance site) when it fails. Weighted 50/50 would send half the traffic to the maintenance page at all times, and latency or geolocation policies route on performance or location, not health-based failover."},
  {"id": 230, "domain": 2, "q": "A VPC spans three Availability Zones, each with private subnets whose instances reach the internet through a single NAT Gateway located in AZ-a. The architect wants outbound internet access to survive the failure of any single AZ. What should they change?", "options": ["Deploy one NAT Gateway in each AZ and configure each private subnet's route table to use the NAT Gateway in its own AZ", "Move the NAT Gateway to AZ-b", "Replace the NAT Gateway with an Internet Gateway in each subnet", "Add a second Elastic IP to the existing NAT Gateway"], "correct": 0, "explanation": "A NAT Gateway is a zonal resource: if AZ-a fails, all private subnets lose internet access. One NAT Gateway per AZ, with zone-local routing, removes this cross-AZ dependency. Moving it just shifts the single point of failure, an Internet Gateway does not provide NAT for private instances, and an extra EIP does not change the zonal nature of the gateway."},
  {"id": 231, "domain": 2, "q": "A legacy application runs on a single EC2 instance and cannot be load-balanced. The team wants the instance to be automatically restored, keeping its instance ID, private IP, and Elastic IP, if the underlying hardware fails. Which mechanism provides this?", "options": ["A CloudWatch alarm on StatusCheckFailed_System with the EC2 'recover' action (or simplified automatic recovery)", "An Auto Scaling group with minimum 2 instances", "A golden AMI restored manually after each failure", "EBS snapshots every hour"], "correct": 0, "explanation": "EC2 automatic recovery, triggered by a system status check failure, migrates the instance to healthy hardware while preserving the instance ID, private IP, Elastic IP, and attached EBS volumes — ideal for a non-scalable legacy workload. An ASG with 2 instances requires the app to support multiple nodes. Manual restore and snapshots increase downtime and effort."},
  {"id": 232, "domain": 2, "q": "When an Auto Scaling group scales in, instances are terminated before they can upload their local log files, causing data loss. How can the team reliably run a log-upload task before each termination?", "options": ["Use an Auto Scaling lifecycle hook on instance termination to pause the instance in Terminating:Wait and run the upload before completing the lifecycle action", "Increase the cooldown period", "Disable scale-in entirely", "Run the upload script every 12 hours via cron"], "correct": 0, "explanation": "A termination lifecycle hook holds the instance in the Terminating:Wait state, giving a script (often triggered through EventBridge/SSM) time to upload the logs before the termination completes. Cooldown only spaces out scaling activities. Disabling scale-in wastes money, and a 12-hour cron still loses up to 12 hours of logs."},
  {"id": 233, "domain": 2, "q": "A company must implement disaster recovery to AWS for 50 on-premises virtual machines, with an RPO of seconds and an RTO of minutes, while keeping ongoing costs low by not running full-size compute until a failover occurs. Which AWS service is designed for this?", "options": ["AWS Elastic Disaster Recovery (DRS) with continuous block-level replication to a low-cost staging area", "AWS Backup with daily on-premises backups", "Manual AMI creation for each VM each week", "S3 Cross-Region Replication"], "correct": 0, "explanation": "Elastic Disaster Recovery continuously replicates on-premises servers at the block level into a low-cost staging area (small instances and EBS), and launches full-size recovery instances only at failover or drill time, achieving second-level RPO and minute-level RTO. Daily backups give an RPO of up to 24 hours, weekly AMIs are worse, and S3 CRR replicates objects, not servers."},
  {"id": 234, "domain": 2, "q": "An on-premises application writes files through SMB and must keep working unchanged, while the company wants the files durably stored in Amazon S3 with frequently used data cached locally for low latency. Which solution fits?", "options": ["AWS Storage Gateway — Amazon S3 File Gateway exposing SMB shares backed by S3 with a local cache", "Mount S3 directly with the AWS CLI on each server", "Amazon EFS mounted over the internet", "AWS Snowball Edge permanently kept on site"], "correct": 0, "explanation": "S3 File Gateway presents NFS/SMB shares to on-premises applications, stores the files as native S3 objects, and caches frequently accessed data locally for low latency — no application change required. S3 is not a POSIX/SMB file system mountable through the CLI, EFS over the internet is NFS (not SMB) and adds latency, and Snowball Edge targets data transfer/edge compute, not a permanent gateway."},
  {"id": 235, "domain": 2, "q": "A company has 40 VPCs across several accounts plus an on-premises data center connected through Direct Connect. Managing the full mesh of VPC peering connections has become unmanageable. Which service simplifies this connectivity with a hub-and-spoke model?", "options": ["AWS Transit Gateway attached to the VPCs and the Direct Connect gateway", "More VPC peering connections", "PrivateLink endpoints between every pair of VPCs", "A VPN between each pair of VPCs"], "correct": 0, "explanation": "Transit Gateway acts as a regional network hub: each VPC and the on-premises connection attach to it once, replacing the n(n-1)/2 peering mesh with hub-and-spoke routing and route-table-based segmentation. More peering aggravates the problem, PrivateLink exposes individual services rather than networks, and pairwise VPNs recreate the same unmanageable mesh."},
  {"id": 236, "domain": 2, "q": "A session cache runs on ElastiCache for Redis in a single node. If the node fails, all sessions are lost and the application is degraded for several minutes. How can you make the cache highly available with automatic failover?", "options": ["Enable replication with a replica in another AZ and Multi-AZ automatic failover on the replication group", "Schedule hourly backups of the Redis node", "Switch to a larger node type", "Move the node to the same AZ as the application"], "correct": 0, "explanation": "An ElastiCache for Redis replication group with at least one replica in a different AZ and Multi-AZ automatic failover promotes the replica within seconds to a few minutes if the primary fails, preserving the cached data. Backups help restore but lose recent sessions and take time. A bigger node or co-locating with the app does not address node failure at all."},
  {"id": 237, "domain": 2, "q": "A company connects its data center to AWS through a single Direct Connect link for business-critical traffic. It wants a cost-effective standby path in case the Direct Connect link fails. What do you recommend?", "options": ["An AWS Site-to-Site VPN over the internet as a failover path for the Direct Connect connection", "A second router in the same Direct Connect location", "An S3 Transfer Acceleration endpoint", "Nothing — Direct Connect is automatically redundant"], "correct": 0, "explanation": "A Site-to-Site VPN provides an encrypted backup path over the internet at low cost, with BGP routing preferring Direct Connect while it is healthy and failing over to the VPN if it goes down. A second router in the same location still shares the same circuit/location risk, Transfer Acceleration is unrelated to hybrid connectivity, and a single Direct Connect link is not automatically redundant."},
  {"id": 238, "domain": 2, "q": "A team wants to protect the objects of a critical S3 bucket against accidental or malicious permanent deletion by compromised credentials. Which features directly reduce this risk? (Choose TWO answers.)", "multi": true, "options": ["Enable S3 Versioning so deletions create delete markers instead of removing data", "Enable MFA Delete so permanently deleting versions requires multi-factor authentication", "Enable S3 Transfer Acceleration", "Switch the bucket to S3 One Zone-IA", "Enable static website hosting"], "correct": [0, 1], "explanation": "Versioning preserves previous versions and turns deletions into recoverable delete markers, and MFA Delete additionally requires an MFA code to permanently delete a version or disable versioning, blocking a compromised access key alone. Transfer Acceleration concerns upload speed, One Zone-IA actually reduces durability, and website hosting is unrelated to deletion protection."},

  {"id": 326, "domain": 3, "q": "An application needs the simplest possible in-memory cache for ephemeral, non-critical data, with the ability to scale horizontally by adding nodes and to use multiple CPU cores per node. Persistence and replication are NOT required. Which engine is the MOST appropriate?", "options": ["ElastiCache for Memcached", "ElastiCache for Redis with cluster mode and AOF persistence", "Amazon DynamoDB", "Amazon Aurora"], "correct": 0, "explanation": "Memcached is a simple, multithreaded cache that scales horizontally by adding nodes — ideal when persistence, replication, and rich data structures are unnecessary. Redis with cluster mode and persistence adds capabilities (and complexity) the requirement explicitly excludes. DynamoDB and Aurora are databases, not microsecond in-memory caches."},
  {"id": 327, "domain": 3, "q": "A DynamoDB table stores orders with the partition key customer_id and sort key order_date. A new feature must efficiently query all orders by product_id, an attribute that is not part of the primary key. Full-table Scans are too slow and expensive. What should you add?", "options": ["A Global Secondary Index (GSI) with product_id as the partition key", "A Local Secondary Index (LSI) with product_id as the sort key", "A second DynamoDB table updated manually", "Parallel Scans with a filter expression"], "correct": 0, "explanation": "A GSI defines a completely different partition (and optional sort) key, enabling efficient Query operations by product_id. An LSI must keep the table's partition key (customer_id) and can only change the sort key, so it cannot serve queries by product alone. A manually synced table is error-prone, and Scans with filters still read the entire table."},
  {"id": 328, "domain": 3, "q": "A REST API on Amazon API Gateway serves a product catalog whose responses change at most every few minutes. The backend Lambda functions and database are heavily loaded by identical repeated GET requests. How can you reduce backend load and latency with minimal changes?", "options": ["Enable API Gateway stage caching with an appropriate TTL for the GET methods", "Increase the Lambda timeout", "Move the API to a larger EC2 instance", "Enable AWS X-Ray tracing"], "correct": 0, "explanation": "API Gateway can cache endpoint responses at the stage level with a configurable TTL, serving repeated identical GETs from the cache without invoking the backend, which cuts both latency and load. A longer Lambda timeout changes nothing for repeated calls, API Gateway does not run on your EC2 instances, and X-Ray observes performance but does not improve it."},
  {"id": 329, "domain": 3, "q": "An Aurora MySQL cluster has one writer and three Aurora Replicas. Developers currently hardcode the endpoint of a single replica for reports, leaving the other replicas idle. How should read traffic be distributed across all replicas with the LEAST effort?", "options": ["Point the reporting applications at the cluster's reader endpoint", "Create a Network Load Balancer in front of the replicas", "Implement client-side round-robin between the three instance endpoints", "Convert the replicas to read-only RDS instances"], "correct": 0, "explanation": "The Aurora reader endpoint automatically load-balances connections across all available Aurora Replicas and transparently accounts for replicas being added, removed, or promoted. An NLB or client-side round-robin adds components and must track replica membership manually, and the replicas are already Aurora read-scaling instances — no conversion makes sense."},
  {"id": 330, "domain": 3, "q": "A data lake application makes a very high rate of GET requests to a single S3 bucket and starts receiving 503 Slow Down errors during peaks, with all objects stored under a single key prefix. What is the recommended way to scale request throughput?", "options": ["Distribute the objects across multiple key prefixes, since S3 supports thousands of requests per second PER PREFIX, and parallelize the requests", "Enable S3 Versioning", "Move the bucket to a larger Region", "Convert the objects to a single large archive file"], "correct": 0, "explanation": "S3 supports at least 3,500 PUT/COPY/POST/DELETE and 5,500 GET/HEAD requests per second per prefix, and there is no limit on the number of prefixes — spreading keys across prefixes and parallelizing requests scales throughput horizontally. Versioning, Region choice, and archiving objects into one file do not raise the per-prefix request ceiling (the last one even hurts parallel access)."},
  {"id": 331, "domain": 3, "q": "A CPU-bound AWS Lambda function that resizes images takes 12 seconds per invocation at 512 MB of memory, which is too slow. The code is already optimized. What is the simplest way to make it run significantly faster?", "options": ["Increase the function's memory allocation, which proportionally increases the allocated vCPU", "Reduce the memory to 128 MB", "Switch the trigger from S3 to SQS", "Increase the function timeout to 15 minutes"], "correct": 0, "explanation": "Lambda allocates CPU power proportionally to the configured memory (up to 6 vCPUs at 10 GB), so raising the memory directly speeds up CPU-bound work and can even lower cost since billing is duration × memory. Reducing memory slows it further, changing the trigger does not affect compute speed, and a longer timeout only allows slowness, it does not fix it."},
  {"id": 332, "domain": 3, "q": "A company must migrate 80 TB from an on-premises NFS array to Amazon S3 over a 10 Gbps dedicated link, with incremental synchronization, data validation, and bandwidth throttling, all managed from AWS. Which service should it use?", "options": ["AWS DataSync with an on-premises agent", "AWS Snowball Edge", "S3 Transfer Acceleration with the CLI", "AWS Database Migration Service (DMS)"], "correct": 0, "explanation": "DataSync is purpose-built for online transfers from NFS/SMB/HDFS to S3, EFS, or FSx: it parallelizes transfers (up to ~10x faster than open-source tools), validates data integrity, supports scheduling, incremental syncs, and bandwidth limits. Snowball is for offline transfer when the network is insufficient (10 Gbps can move 80 TB in about a day). Transfer Acceleration speeds up uploads but offers no managed sync/validation, and DMS migrates databases, not file shares."},
  {"id": 333, "domain": 3, "q": "A research lab must move 600 TB of data to S3 within three weeks, but its internet uplink is only 200 Mbps and cannot be upgraded. Which migration approach is realistic?", "options": ["Order multiple AWS Snowball Edge Storage Optimized devices, load the data locally, and ship them to AWS", "Upload over the existing link with S3 Transfer Acceleration", "Use DataSync over the 200 Mbps link", "Compress the data and upload it with multipart upload"], "correct": 0, "explanation": "At 200 Mbps, 600 TB would take well over 8 months to upload, so any online method (Transfer Acceleration, DataSync, multipart) misses the deadline regardless of optimization. Snowball Edge devices (up to ~80 TB usable each) are loaded locally in parallel and shipped to AWS, where data is imported to S3 within days of receipt."},
  {"id": 334, "domain": 3, "q": "A mission-critical SAP database on EC2 requires up to 200,000 IOPS and sub-millisecond latency on a single EBS volume, with 99.999% durability. Which volume type meets these requirements?", "options": ["io2 Block Express", "gp3", "st1", "gp2 at maximum size"], "correct": 0, "explanation": "io2 Block Express supports up to 256,000 IOPS and 4,000 MB/s per volume with sub-millisecond latency and 99.999% durability, designed precisely for the largest critical databases (SAP HANA, Oracle...). gp3 caps at 16,000 IOPS, st1 is a throughput-oriented HDD, and gp2 tops out at 16,000 IOPS regardless of size."},
  {"id": 335, "domain": 3, "q": "Five independent applications consume the same Kinesis data stream. Consumers experience growing latency because they share each shard's 2 MB/s read throughput and compete in polling. How can EACH consumer get dedicated throughput with push-based, ~70 ms delivery?", "options": ["Register the consumers with enhanced fan-out (dedicated 2 MB/s per consumer per shard over HTTP/2)", "Reduce the number of shards", "Replace Kinesis with SQS Standard", "Increase the polling interval of each consumer"], "correct": 0, "explanation": "Enhanced fan-out gives each registered consumer its own 2 MB/s of throughput per shard, with records pushed over HTTP/2 at roughly 70 ms latency, eliminating contention between consumers. Fewer shards reduces total throughput, SQS does not support multiple independent consumers replaying the same stream, and longer polling intervals increase latency."},
  {"id": 336, "domain": 3, "q": "A website served through CloudFront needs to normalize and rewrite the Host header and append a security header on EVERY viewer request, with microsecond-scale latency and at very large scale. The logic is a few lines of JavaScript with no network calls. Which option is the MOST suitable and economical?", "options": ["CloudFront Functions on viewer request/response events", "Lambda@Edge with 5 minutes of timeout", "An EC2 fleet in each Region", "An ALB rule with header modification"], "correct": 0, "explanation": "CloudFront Functions execute lightweight JavaScript at all CloudFront edge locations with sub-millisecond startup, built for exactly this kind of header manipulation at a fraction of the cost of Lambda@Edge. Lambda@Edge suits heavier logic needing network calls or longer execution. EC2 fleets forfeit edge execution, and ALB rules act after CloudFront, not at the edge."},
  {"id": 337, "domain": 3, "q": "A company centralizes application logs and needs full-text search across terabytes of log data, near-real-time indexing, and interactive dashboards for the operations team. Which AWS service is designed for this use case?", "options": ["Amazon OpenSearch Service with OpenSearch Dashboards", "Amazon RDS for MySQL with LIKE queries", "S3 Select", "AWS CloudTrail"], "correct": 0, "explanation": "Amazon OpenSearch Service provides distributed full-text indexing and search with near-real-time ingestion and built-in OpenSearch Dashboards for visualization — the standard choice for log analytics. LIKE queries on RDS do not scale to terabytes of text, S3 Select queries individual objects without indexing, and CloudTrail records API calls rather than serving as a search engine."},
  {"id": 338, "domain": 3, "q": "A social networking feature must answer queries such as 'friends of my friends who like the same pages' over billions of highly connected relationships, with millisecond latency. Relational JOINs have become far too slow. Which purpose-built database should you choose?", "options": ["Amazon Neptune (graph database)", "Amazon Redshift", "Amazon DynamoDB", "Amazon ElastiCache for Redis"], "correct": 0, "explanation": "Neptune is a purpose-built graph database optimized for traversing billions of relationships with millisecond latency using Gremlin or SPARQL/openCypher — exactly the friends-of-friends pattern. Redshift targets analytical aggregations, DynamoDB is key-value and would require many sequential round trips for multi-hop traversals, and Redis is a cache without graph query semantics."},

  {"id": 421, "domain": 4, "q": "An application writes millions of small objects to S3 Standard that are processed and then deleted after about 10 days. To 'save money,' a junior engineer proposes a Lifecycle rule transitioning the objects to S3 Standard-IA after 7 days. Why is this a BAD idea, and what is better?", "options": ["Standard-IA carries a 30-day minimum storage charge and retrieval fees, so the transition would INCREASE costs; simply keep Standard and use a Lifecycle expiration rule at 10 days", "Standard-IA is always cheaper, the proposal is correct", "Objects cannot be deleted from Standard-IA", "Standard-IA only accepts objects larger than 1 GB"], "correct": 0, "explanation": "Standard-IA bills a minimum of 30 days of storage per object (plus a 128 KB minimum object size and per-GB retrieval fees), so objects deleted at day 10 would still pay 30 days of IA storage plus transition request costs. For short-lived objects, the cost-optimal pattern is staying in Standard with a Lifecycle expiration rule. Objects in IA can of course be deleted, and there is no 1 GB minimum."},
  {"id": 422, "domain": 4, "q": "A cost review shows significant 'EC2-Other' data transfer charges. Investigation reveals two chatty microservices exchanging terabytes of traffic between EC2 instances located in different Availability Zones of the same Region. Which statement about reducing this cost is CORRECT?", "options": ["Data transfer between AZs is billed in each direction, while traffic between instances in the same AZ over private IPs is free, so co-locating the two services in one AZ removes this charge (at the cost of reduced AZ resilience)", "All traffic within a Region is always free", "Using public IPs between the instances would be cheaper", "An Internet Gateway would remove the charges"], "correct": 0, "explanation": "Cross-AZ data transfer within a Region is billed (typically $0.01/GB in each direction), whereas same-AZ traffic over private IPs is free, so co-locating chatty services in one AZ eliminates the charge — a deliberate cost/resilience trade-off. Intra-Region traffic is NOT always free, and communicating over public IPs or through an Internet Gateway costs more, not less."},
  {"id": 423, "domain": 4, "q": "A fault-tolerant big data workload runs on a fleet of Spot Instances, but the team suffers frequent simultaneous interruptions because the fleet requests a single instance type in a single AZ. Which configuration reduces both interruptions and cost?", "options": ["Diversify across many instance types and AZs and use the price-capacity-optimized allocation strategy", "Request all capacity in the cheapest single Spot pool", "Switch the whole fleet to On-Demand", "Use only the largest available instance type"], "correct": 0, "explanation": "Diversifying across instance types and AZs with the price-capacity-optimized strategy makes AWS pick the pools with the deepest spare capacity at low prices, sharply reducing simultaneous interruptions while preserving Spot savings. Concentrating in one cheap pool maximizes interruption risk, full On-Demand multiplies cost, and a single large type recreates the single-pool problem."},
  {"id": 424, "domain": 4, "q": "Compliance archives are stored in S3 Glacier Flexible Retrieval. During an incident investigation, the security team occasionally needs to retrieve a few specific archives in under 5 minutes, while routine retrievals can wait hours. What is the MOST cost-effective approach?", "options": ["Keep the data in Glacier Flexible Retrieval and use Expedited retrievals (1-5 minutes) only for the urgent requests", "Move all archives to S3 Standard", "Move all archives to Glacier Deep Archive", "Duplicate all archives in EBS volumes"], "correct": 0, "explanation": "Glacier Flexible Retrieval supports three retrieval tiers — Expedited (1-5 min), Standard (3-5 h), and Bulk (5-12 h) — so paying the Expedited premium only for the rare urgent archives keeps storage costs low. Moving everything to S3 Standard multiplies the storage cost for data that is almost never read, Deep Archive cannot retrieve in minutes (12+ hours), and duplicating to EBS is the most expensive option."},
  {"id": 425, "domain": 4, "q": "A FinOps team wants AWS costs broken down by project and by team in Cost Explorer and in the Cost and Usage Report. Resources are already tagged with Project and Team tags, but these dimensions do not appear in the billing tools. What MUST be done?", "options": ["Activate the user-defined cost allocation tags in the Billing console", "Recreate all resources", "Open a support ticket so AWS enables the tags", "Rename the tags to lowercase"], "correct": 0, "explanation": "User-defined tags only appear as billing dimensions after being explicitly activated as cost allocation tags in the Billing and Cost Management console (activation applies to usage going forward). No resource recreation or support ticket is needed, and tag case does not prevent activation."},
  {"id": 426, "domain": 4, "q": "A DynamoDB table serves steady, predictable traffic of about 8,000 reads and 2,000 writes per second, 24/7, currently in on-demand capacity mode. The monthly bill is high. How can costs be reduced for this stable workload?", "options": ["Switch to provisioned capacity mode with auto scaling, and consider reserved capacity for the stable baseline", "Stay on on-demand mode, it is always the cheapest", "Add a Global Secondary Index", "Enable point-in-time recovery"], "correct": 0, "explanation": "On-demand mode charges a premium per request in exchange for zero capacity planning, which is wasteful for steady predictable traffic; provisioned capacity with auto scaling is markedly cheaper at constant high utilization, and reserved capacity discounts the baseline further. A GSI adds storage and write costs, and point-in-time recovery is a (paid) backup feature, not a cost reduction."},
  {"id": 427, "domain": 4, "q": "A Lambda function is configured with 10 GB of memory 'to be safe.' Monitoring shows it uses at most 400 MB and is I/O-bound (waiting on API calls), with billing dominated by GB-seconds. What is the MOST direct way to cut its cost without hurting behavior?", "options": ["Lower the memory allocation to a value modestly above actual usage (e.g., 512 MB), since Lambda bills duration multiplied by configured memory", "Raise the memory to 10,240 MB", "Increase the timeout", "Switch the function to a container image"], "correct": 0, "explanation": "Lambda pricing is GB-seconds (configured memory × duration): an I/O-bound function waiting on external calls gains nothing from extra CPU, so right-sizing memory close to real usage divides the cost by up to ~20x here. More memory raises cost for no benefit on I/O waits, the timeout does not affect billing rate, and packaging format does not change pricing."},
  {"id": 428, "domain": 4, "q": "A company keeps hundreds of EBS snapshots for compliance, retained for 2 years but almost never restored. Snapshot storage costs keep growing. Which option reduces these costs while keeping the snapshots restorable?", "options": ["Move the rarely accessed snapshots to the EBS Snapshots Archive tier (up to ~75% cheaper), accepting a 24-72 hour restore time", "Delete every snapshot older than 30 days", "Copy each snapshot to another Region", "Convert the snapshots into AMIs"], "correct": 0, "explanation": "EBS Snapshots Archive stores full snapshots at up to 75% lower cost than the standard tier, well suited to compliance copies that are rarely restored, with retrieval taking 24-72 hours. Deleting snapshots violates the 2-year retention requirement, cross-Region copies double the cost, and AMI conversion still stores the same underlying snapshot data."},
  {"id": 429, "domain": 4, "q": "An audit finds that CloudWatch Logs spending keeps growing because every log group uses the default 'Never expire' retention, while the company only needs 30 days of searchable logs and long-term copies for compliance. Which combination minimizes cost?", "options": ["Set a 30-day retention policy on the log groups and export/route older logs to S3 with lifecycle transitions to archive storage classes", "Keep all logs in CloudWatch Logs forever", "Print critical logs to PDF monthly", "Double the CloudWatch Logs ingestion"], "correct": 0, "explanation": "CloudWatch Logs storage is significantly more expensive per GB than S3; setting a 30-day retention on log groups caps the costly tier, while exporting or streaming older logs to S3 (then Glacier classes via lifecycle rules) satisfies long-term compliance cheaply. Keeping everything in CloudWatch Logs forever is exactly the current problem, and the other options are not serious cost controls."},
  {"id": 430, "domain": 4, "q": "A genomics company hosts a multi-petabyte public dataset in S3. External research partners download heavily, and the company wants to stop paying the data transfer and request costs generated by those downloads. Which S3 feature shifts these costs to the consumers?", "options": ["Enable Requester Pays on the bucket, so authenticated requesters are billed for the requests and data transfer", "Enable S3 Transfer Acceleration", "Make the bucket fully public with anonymous access", "Move the dataset to S3 One Zone-IA"], "correct": 0, "explanation": "With Requester Pays, the downloader (who must authenticate with AWS credentials) is billed for the request and data transfer costs, while the owner keeps paying only for storage — the standard pattern for large shared datasets. Transfer Acceleration adds cost to the owner, anonymous public access leaves all costs with the owner (and Requester Pays even forbids anonymous access), and One Zone-IA cuts durability without touching transfer costs."},
  {"id": 431, "domain": 4, "q": "A company runs about 60 development and test EC2 instances that are only needed on weekdays between 8 AM and 7 PM, and several of them are heavily over-provisioned. Which actions reduce cost the MOST? (Choose TWO answers.)", "multi": true, "options": ["Automatically stop the instances outside business hours (e.g., Instance Scheduler or EventBridge + Lambda)", "Right-size the over-provisioned instances using AWS Compute Optimizer recommendations", "Enable Multi-AZ on all development databases", "Migrate all the instances to Dedicated Hosts", "Upgrade every instance to the latest largest instance type"], "correct": [0, 1], "explanation": "Stopping instances nights and weekends eliminates roughly two-thirds of their compute hours, and right-sizing based on Compute Optimizer's utilization analysis removes the over-provisioning premium — together these are the highest-impact actions. Multi-AZ increases cost for dev environments, Dedicated Hosts are the most expensive tenancy, and upgrading to larger types raises spend."}
);
